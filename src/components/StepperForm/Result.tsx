// https://codesandbox.io/s/react-hook-form-wizard-form-forked-l23xlh

// https://ir780.csb.app/result : as seen here all data from each steps saved into a single object
const Result = () => {
  // now simply get the data from the store and display here as needed
  // const { state } = useStateMachine(updateAction);

  return (
    <>
      <h2>Result:</h2>
      {/* <pre>{JSON.stringify(state, null, 2)}</pre> */}
    </>
  );
};

export default Result;
